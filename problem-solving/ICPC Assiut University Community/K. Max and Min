#include<iostream>
#include<algorithm>

using namespace std;

int main()
{
    long long x,y,z,minimum,maximum;
    
    cin >> x >> y >> z;
    
    maximum = max(max(x,y), z);
    minimum = min(min(x,y), z);
    
    cout << minimum << " " << maximum;
}
