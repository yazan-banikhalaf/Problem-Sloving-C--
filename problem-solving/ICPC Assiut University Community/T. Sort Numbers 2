#include <iostream>
#include <cmath>
#include <algorithm>

using namespace std;

int main()
{
    int x, y, z;
    cin >> x >> y >> z;

    int numbers[3] = {x,y,z};
    
    sort(numbers, numbers + 3);
    
    cout << numbers[0] << endl;
    cout << numbers[1] << endl;
    cout << numbers[2] << endl;
    
    cout << endl;
    
    cout << x << endl;
    cout << y << endl;
    cout << z << endl;
    return 0;
}
