#include<bits/stdc++.h>
using namespace std;


int main() {
    
    int number1, number2, size, gcd;
    
    cin >> number1 >> number2;
    
    size = max(number1,number2);
    
    for(int i = 1; i <= size; i++) {
        if(number1 % i == 0 && number2 % i == 0) {
            gcd = i;
        }
    }
    cout << gcd;
    return 0;
}
