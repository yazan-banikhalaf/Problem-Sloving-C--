#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, values;
    cin >> n;
    for(int i = 1; i <= n; i++) {
        if( i % 2 == 0) {
            cout << i << "\n";
        } else if (n == 1) {
            cout << -1;
        }
    }
    return 0;
}
