#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long a ,b ,k;
    
    cin >> a >> b >> k;
    
    bool aDivisiable = a % k == 0;
    bool bDivisiabke = b % k == 0;
    
    if(aDivisiable && bDivisiabke) {
        cout << "Both" << endl;
    } else if (aDivisiable && !bDivisiabke) {
        cout << "Memo" << endl;
    } else if (!aDivisiable && bDivisiabke) {
        cout << "Momo" << endl;
    } else {
        cout << "No One" << endl;
    }
    return 0;
}
